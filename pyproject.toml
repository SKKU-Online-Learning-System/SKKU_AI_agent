[project]
name = "skku-course-agent-workspace"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "apps/backend",
  "packages/ai_rag",
]

[tool.pytest.ini_options]
testpaths = ["apps/backend/tests", "packages/ai_rag/tests"]
pythonpath = ["apps/backend", "packages/ai_rag"]
