# 비워 두면 웹 UI와 같은 origin의 /api rewrite로 API를 호출합니다(Backend.AI에서 run.sh가 이렇게 둡니다).
# 브라우저가 API를 직접 호출하게 하려면 8000 preopen 앱의 공개 주소를 넣습니다.
# 예: NEXT_PUBLIC_API_BASE_URL=https://siriuscluster.skku.edu:102xx
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
